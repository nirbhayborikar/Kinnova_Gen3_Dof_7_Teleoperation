# =============================================================================
# kinova-follower / .env
# Must match the kortex stack values
# =============================================================================

# Must be identical to what common.yaml sets for the kortex stack
ROS_DOMAIN_ID=145